name: Version Update

on:
  pull_request:
    branches:
      - main

jobs:
  determine-new-version:
    uses: seamuslowry/workflows/.github/workflows/tag.yml@main
    with:
      dry-run: true
  ensure-new-version:
    runs-on: ubuntu-latest
    needs: determine-new-version
    steps:
      # set up
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17

      # main branch versions
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.base_ref }}
      - run: |
          echo "version-name=$(./gradlew --no-daemon -q printVersionName)" >> "$GITHUB_OUTPUT"
          echo "version-code=$(./gradlew --no-daemon -q printVersionCode)" >> "$GITHUB_OUTPUT"
        id: main-branch-versions

      - name: Update versions
        run: |
          sed -i '' "s/versionName \"${{ steps.main-branch-versions.outputs.version-name }}\"/versionName \"${{ needs.determine-new-version.outputs.tag }}\"/" app/build.gradle
          sed -i '' "s/versionCode ${{ steps.main-branch-versions.outputs.version-code }}/versionCode $(( ${{ steps.main-branch-versions.outputs.version-code }} + 1 ))/" app/build.gradle
          git diff
